#include <stdio.h>
#include <ctype.h>

int main()
{
	int c;

	for(c = 0; c < 128; c++) {

//		printf("%c is %salphanumeric\n", c, isalnum(c) ? "    " : "not ");

//		printf("%c is %salpha\n", c, isalpha(c) ? "    " : "not ");
//
//		printf("%c is %sascii\n", c, isascii(c) ? "    " : "not ");
//
//		printf("%c is %sblank\n", c, isblank(c) ? "    " : "not ");
//
//		printf("%c is %scntrl\n", c, iscntrl(c) ? "    " : "not ");
//
		printf("%c is %sdigit\n", c, isdigit(c) ? "    " : "not ");
//
//		printf("%c is %sgraph\n", c, isgraph(c) ? "    " : "not ");
//
//		printf("%c is %slower\n", c, islower(c) ? "    " : "not ");
//
//		printf("%c is %sprint\n", c, isprint(c) ? "    " : "not ");
//
//		printf("%c is %spunct\n", c, ispunct(c) ? "    " : "not ");
//
//		printf("%c is %sspace\n", c, isspace(c) ? "    " : "not ");
//
//		printf("%c is %supper\n", c, isupper(c) ? "    " : "not ");
//
//		printf("%c is %sx digit\n", c, isxdigit(c) ? "    " : "not ");

		printf("\n");

	}

	return 0;
}
