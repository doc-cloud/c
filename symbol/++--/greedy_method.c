#include <stdio.h>

/* C standard just define the feature, do no efforts on implementation of compiler */
/*
 * read more characters as much as possible, left to right,
 * until one moment that there is no possible for willing read character cannot participe a legal symbol with read character
 */
int main()
{
	int i = 0;

	//int j = ++i+++i+++i;

	int a = 1;
	int b = 2;
	int c;
	printf("%d\n",c = a+++b);

	++i + ++i;
	return 0;
}
